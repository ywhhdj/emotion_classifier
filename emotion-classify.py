from emotion_classifier.cli import main
if __name__ == "__main__":
    # emotion_classifier = EmotionClassifier(
    #     model_dir="models"
    # )
    # texts = ["我非常开心", "我非常生气", "我非常害怕", "我非常想死"]
    # results = emotion_classifier.predict(texts)
    # print(results)
    main()